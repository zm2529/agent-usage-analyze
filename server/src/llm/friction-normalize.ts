// Re-exports from @agent-analytics/cli/analysis/friction-normalize.
// Moved to CLI package so the CLI can use friction normalization for native analysis (--native mode).
export { normalizeFrictionCategory } from '@agent-analytics/cli/analysis/friction-normalize';
