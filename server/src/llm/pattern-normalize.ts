// Re-exports from @agent-analytics/cli/analysis/pattern-normalize.
// Moved to CLI package so the CLI can use pattern normalization for native analysis (--native mode).
export { PATTERN_CATEGORY_LABELS, normalizePatternCategory, getPatternCategoryLabel } from '@agent-analytics/cli/analysis/pattern-normalize';
